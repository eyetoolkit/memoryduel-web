{
  "name": "memoryduel-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MemoryDuel — 记忆对决站（Vite MPA + 发布阶段开关）",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/copy-legacy.mjs && node scripts/filter-sitemap.mjs",
    "build:beta": "VITE_SHOW_BETA=1 tsc --noEmit && VITE_SHOW_BETA=1 vite build && VITE_SHOW_BETA=1 node scripts/copy-legacy.mjs && VITE_SHOW_BETA=1 node scripts/filter-sitemap.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
